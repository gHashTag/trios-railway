# TASK.md — SR-HACK-00: Trinity Glossary

## Goal

Create the dependency-free vocabulary ring that pins L0 of the partnership plan into Rust types. This ring fixes the persistent `pipeline ≠ algorithm` confusion in every PR comment, DM, leaderboard row, and Discord message generated by SR-HACK-01..05.

## Acceptance Criteria

- [x] `crates/trios-igla-race-hack/` GOLD scaffold exists with `RING.md`, `Cargo.toml`, `src/lib.rs` (≤ 50 LoC, re-exports only).
- [x] `crates/trios-igla-race-hack/rings/SR-HACK-00/` contains `README.md`, `TASK.md`, `AGENTS.md`, `Cargo.toml`, `src/lib.rs` (I5 mandatory).
- [x] Cargo deps limited to `serde + serde_json` (R-RING-DEP-002 future doctor rule).
- [x] Public types: `enum RingTier { Gold, Silver, Bronze, ColorVariant(String) }`, `enum Lane { Algorithm, TttLora, Quantization, Megakernels, Theory }`, `enum Gate { G1, G2, G3 }`, `enum Term { PipelineO1, AlgorithmEntry, Lane(Lane), Gate(Gate), RingTier(RingTier) }`, `Display`, full `Serialize/Deserialize` roundtrip.
- [x] Unit tests: roundtrip JSON for every variant + display string contracts.
- [x] `cargo test -p trios-igla-race-hack` green in < 5 s.
- [x] `cargo clippy --all-targets -- -D warnings` clean (L3).
- [ ] PR closes #447 (L2), commit trailer `Agent: GAMMA` (L14).
- [x] `.trinity/experience/trios_<DATE>.trinity` line written (L7).

## Non-Goals

- No connection to live fleet or production systems
- No runtime dependencies beyond serde/serde_json
- No `.sh` files (L1)

## References

- EPIC #446 — Ring-Pattern Refactor
- LAWS.md v2.0 — Agent dispatch system
- Issue #236 — HEARTBEAT format

## Status

- DONE: Types implemented with nested enum structure
- DONE: Unit tests added (3 tests, JSON roundtrip + display)
- DONE: I5 files complete (README.md, TASK.md, AGENTS.md, Cargo.toml, src/lib.rs)
- DONE: cargo test green, cargo clippy clean
- PENDING: PR creation → closes #447
